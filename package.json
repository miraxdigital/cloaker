{
  "name": "router-redirect-system",
  "version": "1.0.0",
  "description": "Sistema de redirecionamento baseado em parâmetros",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "build": "mkdir -p dist && echo \"Static site - no build required\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}